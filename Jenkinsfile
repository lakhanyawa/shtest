pipeline {
    agent {
      dockerfile true
    }
    stages { 
       stage('dockertest') {
           steps {
              echo "This is testing doker build"
              echo "Build Completed"
            }
        }
     }
}
